allprojects{
    group = 'com.avevad'
    version = '0.1-SNAPSHOT'
}

subprojects{
    apply plugin: "java"

    compileJava{
        options.fork = true
        options.incremental = true
    }

    sourceCompatibility = 1.8
    targetCompatibility = 1.8
}
